- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.vimrc:
    ~/.git-completion.zsh:

- create:
    - ~/Work

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
